<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              NavigationPage.HasNavigationBar="False"
             x:Class="SixOSDatKhamAppMobile.Pages.S0306_mXacThucOTP">

    <Grid>
        <!-- Overlay mờ -->
        <BoxView x:Name="Overlay"
                 BackgroundColor="#80000000"
                 Opacity="0"
                 IsVisible="False"/>

        <!-- Form OTP 4 số -->
        <Border x:Name="OtpFormContainer"
                IsVisible="False"
                BackgroundColor="White"
                StrokeShape="RoundRectangle 20"
                Stroke="#007bff"
                StrokeThickness="2"
                Padding="30"
                WidthRequest="380"
                HeightRequest="440"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                ZIndex="1000"
                Scale="0.9"
                Opacity="0">

            <VerticalStackLayout Spacing="20">

                <!-- Tiêu đề OTP -->
                <VerticalStackLayout Spacing="8"
                                     HorizontalOptions="Center">
                    <Label Text="🔐 XÁC THỰC OTP"
                           TextColor="#007bff"
                           FontSize="22"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>

                    <Label x:Name="OtpPhoneInfo"
                           Text="Mã OTP 4 số đã gửi đến: 0912***789"
                           TextColor="#666666"
                           FontSize="14"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>

                <!-- OTP Input - 4 số -->
                <VerticalStackLayout Spacing="20">
                    <Label Text="Nhập mã OTP 4 số"
                           TextColor="#333333"
                           FontSize="16"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>

                    <!-- OTP Boxes - 4 ô -->
                    <HorizontalStackLayout HorizontalOptions="Center"
                                           Spacing="15">

                        <Border x:Name="OtpBox1"
                                WidthRequest="60"
                                HeightRequest="60"
                                Stroke="#007bff"
                                StrokeThickness="2"
                                StrokeShape="RoundRectangle 12"
                                BackgroundColor="#F5F5F5">
                            <Entry x:Name="OtpDigit1"
                                   Keyboard="Numeric"
                                   FontSize="24"
                                   TextColor="#333333"
                                   HorizontalTextAlignment="Center"
                                   VerticalTextAlignment="Center"
                                   MaxLength="1"
                                   TextChanged="OnOtpDigitChanged"/>
                        </Border>

                        <Border x:Name="OtpBox2"
                                WidthRequest="60"
                                HeightRequest="60"
                                Stroke="#E0E0E0"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 12"
                                BackgroundColor="#F5F5F5">
                            <Entry x:Name="OtpDigit2"
                                   Keyboard="Numeric"
                                   FontSize="24"
                                   TextColor="#333333"
                                   HorizontalTextAlignment="Center"
                                   VerticalTextAlignment="Center"
                                   MaxLength="1"
                                   TextChanged="OnOtpDigitChanged"/>
                        </Border>

                        <Border x:Name="OtpBox3"
                                WidthRequest="60"
                                HeightRequest="60"
                                Stroke="#E0E0E0"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 12"
                                BackgroundColor="#F5F5F5">
                            <Entry x:Name="OtpDigit3"
                                   Keyboard="Numeric"
                                   FontSize="24"
                                   TextColor="#333333"
                                   HorizontalTextAlignment="Center"
                                   VerticalTextAlignment="Center"
                                   MaxLength="1"
                                   TextChanged="OnOtpDigitChanged"/>
                        </Border>

                        <Border x:Name="OtpBox4"
                                WidthRequest="60"
                                HeightRequest="60"
                                Stroke="#E0E0E0"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 12"
                                BackgroundColor="#F5F5F5">
                            <Entry x:Name="OtpDigit4"
                                   Keyboard="Numeric"
                                   FontSize="24"
                                   TextColor="#333333"
                                   HorizontalTextAlignment="Center"
                                   VerticalTextAlignment="Center"
                                   MaxLength="1"
                                   TextChanged="OnOtpDigitChanged"/>
                        </Border>

                    </HorizontalStackLayout>

                    <!-- Timer -->
                    <HorizontalStackLayout HorizontalOptions="Center"
                                           Spacing="12"
                                           Margin="0,5,0,0">

                        <Label x:Name="OtpTimerLabel"
                               Text="Mã OTP có hiệu lực trong: 60s"
                               TextColor="#007bff"
                               FontSize="14"
                               FontAttributes="Bold"/>

                        <Button x:Name="ResendOtpButton"
                                Text="Gửi lại mã"
                                BackgroundColor="Transparent"
                                TextColor="#007bff"
                                FontSize="14"
                                FontAttributes="Bold"
                                Padding="0"
                                IsEnabled="False"
                                Clicked="OnResendOtpClicked"/>

                    </HorizontalStackLayout>
                </VerticalStackLayout>

                <!-- Nút xác nhận OTP -->
                <Button x:Name="VerifyOtpButton"
                        Text="XÁC NHẬN OTP"
                        BackgroundColor="#007bff"
                        TextColor="White"
                        FontSize="16"
                        FontAttributes="Bold"
                        CornerRadius="25"
                        HeightRequest="50"
                        Margin="0,5,0,0"
                        IsEnabled="False"
                        Clicked="OnVerifyOtpClicked"/>

                <!-- Nút đóng OTP form -->
                <Button Text="❌ Quay lại nhập thông tin"
                        BackgroundColor="Transparent"
                        TextColor="#666666"
                        FontSize="13"
                        Padding="0"
                        HorizontalOptions="Center"
                        Margin="0,10,0,0"
                        Clicked="OnHideOtpForm"/>

            </VerticalStackLayout>
        </Border>

    </Grid>
</ContentView>