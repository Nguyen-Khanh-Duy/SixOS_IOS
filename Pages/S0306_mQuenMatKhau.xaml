<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:SixOSDatKhamAppMobile.Pages"
             x:Class="SixOSDatKhamAppMobile.Pages.S0306_mQuenMatKhau"
              NavigationPage.HasNavigationBar="False"
             Title="Quên Mật Khẩu"
             BackgroundColor="{StaticResource BackgroundColor}">

    <Grid>
        <!-- Main content -->
        <ScrollView x:Name="MainScrollView">
            <Grid RowDefinitions="Auto,Auto,Auto,Auto"
                  RowSpacing="0"
                  Padding="20">

                <!-- Header -->
                <VerticalStackLayout Grid.Row="0"
                                     Spacing="15"
                                     HorizontalOptions="Center"
                                     Margin="0,0,0,30">

                    <!-- Logo + Chữ cạnh nhau -->
                    <HorizontalStackLayout Spacing="15"
                                           Padding="0"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center">
                        <Image Source="logo.jpg"
                               Aspect="AspectFit"/>
                        <VerticalStackLayout Spacing="2"
                                             VerticalOptions="Center">
                            <Label Text="Bệnh viện Ung Bướu"
                                   TextColor="{StaticResource PrimaryColor}"
                                   FontSize="20"
                                   FontAttributes="Bold" />
                            <Label Text="Thành phố Hồ Chí Minh"
                                   TextColor="{StaticResource PrimaryColor}"
                                   FontSize="14" />
                        </VerticalStackLayout>
                    </HorizontalStackLayout>

                    <!-- Tiêu đề -->
                    <Label Text="QUÊN MẬT KHẨU"
                           TextColor="{StaticResource PrimaryColor}"
                           FontFamily="MontserratBold"
                           FontSize="28"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>

                    <!-- Mô tả -->
                    <Label Text="Nhập thông tin để khôi phục mật khẩu"
                           TextColor="#666666"
                           FontSize="12"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>

                <!-- Form quên mật khẩu -->
                <Border Grid.Row="1"
                        BackgroundColor="White"
                        StrokeShape="RoundRectangle 15"
                        Stroke="#E0E0E0"
                        StrokeThickness="1"
                        Padding="25">

                    <VerticalStackLayout Spacing="20">

                        <!-- CCCD -->
                        <VerticalStackLayout Spacing="8">
                            <Label Text="Số căn cước công dân/Passport"
                                   TextColor="{StaticResource TextColor}"
                                   FontSize="14"
                                   FontAttributes="Bold"/>

                            <Border Stroke="#E0E0E0"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 10"
                                    BackgroundColor="#F5F5F5"
                                    HeightRequest="50">
                                <Entry x:Name="CccdEntry"
                                       Placeholder="Nhập số CCCD/Passport"
                                       Keyboard="Numeric"
                                       FontSize="16"
                                       TextColor="{StaticResource TextColor}"
                                       PlaceholderColor="#999999"
                                       ClearButtonVisibility="WhileEditing"
                                       Margin="15,0"/>
                            </Border>
                        </VerticalStackLayout>

                        <!-- Số điện thoại -->
                        <VerticalStackLayout Spacing="8">
                            <Label Text="Số điện thoại đăng ký"
                                   TextColor="{StaticResource TextColor}"
                                   FontSize="14"
                                   FontAttributes="Bold"/>

                            <Border Stroke="#E0E0E0"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 10"
                                    BackgroundColor="#F5F5F5"
                                    HeightRequest="50">
                                <Grid ColumnDefinitions="Auto,*">

                                    <!-- Mã quốc gia -->
                                    <HorizontalStackLayout Grid.Column="0"
                                                           Spacing="5"
                                                           VerticalOptions="Center"
                                                           Padding="15,0">
                                        <Label Text="🇻🇳"
                                               FontSize="18"/>
                                        <Label Text="+84"
                                               TextColor="{StaticResource TextColor}"
                                               FontSize="16"/>
                                    </HorizontalStackLayout>

                                    <!-- Entry số điện thoại -->
                                    <Entry Grid.Column="1"
                                           x:Name="PhoneEntry"
                                           Placeholder="Nhập số điện thoại"
                                           Keyboard="Telephone"
                                           FontSize="16"
                                           TextColor="{StaticResource TextColor}"
                                           PlaceholderColor="#999999"
                                           ClearButtonVisibility="WhileEditing"
                                           Margin="0,0,15,0"/>

                                </Grid>
                            </Border>
                        </VerticalStackLayout>

                        <!-- Thông báo gợi ý (ẩn ban đầu) -->
                        <Border x:Name="HintContainer"
                                IsVisible="False"
                                BackgroundColor="#FFF3E0"
                                Stroke="#FFB74D"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 10"
                                Padding="15">
                            <HorizontalStackLayout Spacing="10"
                                VerticalOptions="Center">
                                <Label Text=""
                                    FontSize="16"/>
                                <Label x:Name="HintLabel"
                                       Text=""
                                       TextColor="#EF6C00"
                                       FontSize="13"
                                       LineBreakMode="WordWrap"/>
                            </HorizontalStackLayout>
                        </Border>

                        <!-- Nút tiếp tục -->
                        <Button x:Name="ContinueButton"
                                Text="TIẾP TỤC"
                                BackgroundColor="{StaticResource PrimaryColor}"
                                TextColor="White"
                                FontSize="18"
                                FontAttributes="Bold"
                                CornerRadius="25"
                                HeightRequest="55"
                                Margin="0,10,0,0"
                                Clicked="OnContinueClicked"/>

                    </VerticalStackLayout>
                </Border>

                <!-- Thông tin hỗ trợ -->
                <VerticalStackLayout Grid.Row="2"
                                     Spacing="15"
                                     HorizontalOptions="Center"
                                     Margin="0,30,0,0">

                    <Label Text="Không thể xác thực ?"
                           TextColor="{StaticResource TextColor}"
                           FontSize="14"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>

                    <Button Text="📞 Liên hệ hỗ trợ: 028 3841 2637"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource PrimaryColor}"
                            FontSize="14"
                            Padding="0"
                            HorizontalOptions="Center"
                            Clicked="OnCallSupportClicked"/>

                </VerticalStackLayout>

                <!-- Quay lại đăng nhập -->
                <HorizontalStackLayout Grid.Row="3"
                                       Spacing="5"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"
                                       Margin="0,20,0,30">

                    <Label Text="Đã nhớ mật khẩu ?"
                           TextColor="#666666"
                           FontSize="14"/>

                    <Button Text="Đăng nhập ngay"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource PrimaryColor}"
                            FontSize="14"
                            FontAttributes="Bold"
                            Padding="0"
                            Clicked="OnBackToLoginClicked"/>

                </HorizontalStackLayout>

            </Grid>
        </ScrollView>

        <!-- OTP Popup Component (Reusable) -->
        <pages:S0306_mXacThucOTP x:Name="OtpPopup" />

    </Grid>
</ContentPage>